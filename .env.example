# ╔════════════════════════════════════════════════════════╗
# ║            Environment Variables Template               ║
# ║                                                         ║
# ║  ⚠️  THIS IS A TEMPLATE - CONTAINS NO REAL SECRETS     ║
# ║  Copy to .env and fill in actual values                ║
# ╚════════════════════════════════════════════════════════╝

# ┌─────────────────────────────────────────────────────────┐
# │ 🌍 Environment Configuration                            │
# └─────────────────────────────────────────────────────────┘
ENVIRONMENT=development  # development | staging | production
DEBUG=true
LOG_LEVEL=INFO  # DEBUG | INFO | WARNING | ERROR | CRITICAL

# ┌─────────────────────────────────────────────────────────┐
# │ 🚀 Application Settings                                 │
# └─────────────────────────────────────────────────────────┘
APP_NAME="Enterprise Image Generation Platform"
APP_VERSION=0.1.0
API_V1_PREFIX=/api/v1
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173

# ┌─────────────────────────────────────────────────────────┐
# │ 🔐 Azure Active Directory / Entra ID                    │
# └─────────────────────────────────────────────────────────┘
AZURE_TENANT_ID=your-tenant-id-here
AZURE_CLIENT_ID=your-client-id-here
AZURE_CLIENT_SECRET=your-client-secret-here
AZURE_AUTHORITY=https://login.microsoftonline.com/your-tenant-id-here
AZURE_REDIRECT_URI=http://localhost:8000/auth/callback

# MFA Settings
REQUIRE_MFA=true
MFA_GRACE_PERIOD_DAYS=7

# ┌─────────────────────────────────────────────────────────┐
# │ 🗄️ Database Configuration (Azure SQL)                  │
# └─────────────────────────────────────────────────────────┘
DATABASE_URL=mssql+pyodbc://username:password@server.database.windows.net:1433/dbname?driver=ODBC+Driver+18+for+SQL+Server
# For local development (PostgreSQL):
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/eig_platform

DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_ECHO=false  # Set to true for SQL query logging

# ┌─────────────────────────────────────────────────────────┐
# │ 🌐 Azure Cosmos DB (Audit Logs)                         │
# └─────────────────────────────────────────────────────────┘
COSMOS_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/
COSMOS_KEY=your-cosmos-primary-key-here
COSMOS_DATABASE_NAME=eig-audit-logs
COSMOS_CONTAINER_NAME=audit-events

# ┌─────────────────────────────────────────────────────────┐
# │ 💾 Azure Blob Storage (Images)                          │
# └─────────────────────────────────────────────────────────┘
AZURE_STORAGE_ACCOUNT_NAME=your-storage-account-name
AZURE_STORAGE_ACCOUNT_KEY=your-storage-account-key
AZURE_STORAGE_CONTAINER_NAME=generated-images
AZURE_STORAGE_CDN_ENDPOINT=https://your-cdn-endpoint.azureedge.net

# Storage Tiers
STORAGE_HOT_DAYS=30  # Keep in hot tier for 30 days
STORAGE_COOL_DAYS=60  # Move to cool tier after 60 days
STORAGE_ARCHIVE_DAYS=90  # Archive after 90 days

# ┌─────────────────────────────────────────────────────────┐
# │ 🔑 Azure Key Vault                                      │
# └─────────────────────────────────────────────────────────┘
KEY_VAULT_URL=https://your-keyvault.vault.azure.net/
KEY_VAULT_TENANT_ID=your-tenant-id-here
# Use Managed Identity in production (no client secret needed)
KEY_VAULT_CLIENT_ID=your-managed-identity-client-id
KEY_VAULT_CLIENT_SECRET=only-for-local-dev

# ┌─────────────────────────────────────────────────────────┐
# │ 🤖 AI Model Provider APIs                               │
# └─────────────────────────────────────────────────────────┘

# OpenAI (DALL-E 3)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_ORGANIZATION_ID=org-your-org-id
# For Azure OpenAI Service (recommended for compliance):
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-key
AZURE_OPENAI_DEPLOYMENT_NAME=dalle3-deployment

# Replicate (Stable Diffusion XL)
REPLICATE_API_TOKEN=r8_your-replicate-token-here

# Adobe Firefly
ADOBE_CLIENT_ID=your-adobe-client-id
ADOBE_CLIENT_SECRET=your-adobe-client-secret
ADOBE_API_KEY=your-adobe-api-key

# Azure AI Image (Azure Cognitive Services)
AZURE_AI_ENDPOINT=https://australiaeast.api.cognitive.microsoft.com/
AZURE_AI_API_KEY=your-azure-ai-key

# ┌─────────────────────────────────────────────────────────┐
# │ 💰 Cost Management                                      │
# └─────────────────────────────────────────────────────────┘
DEFAULT_DEPARTMENT_BUDGET_AUD=5000.00  # Monthly budget
BUDGET_ENFORCEMENT=hard  # hard | soft | warn
COST_ALERT_THRESHOLD_PERCENT=80
MAX_COST_PER_IMAGE_AUD=0.50  # Safety limit

# Model Costs (AUD per image)
COST_DALLE3=0.08
COST_SDXL=0.02
COST_FIREFLY=0.10
COST_AZURE_AI=0.05

# ┌─────────────────────────────────────────────────────────┐
# │ 🔍 PII Detection & Content Moderation                   │
# └─────────────────────────────────────────────────────────┘
ENABLE_PII_DETECTION=true
PII_DETECTION_THRESHOLD=0.7  # 0.0 to 1.0 (confidence)
PII_ACTION=block  # block | warn | anonymize

# Azure Content Safety
AZURE_CONTENT_SAFETY_ENDPOINT=https://australiaeast.api.cognitive.microsoft.com/
AZURE_CONTENT_SAFETY_KEY=your-content-safety-key
CONTENT_SAFETY_THRESHOLD=medium  # low | medium | high

# ┌─────────────────────────────────────────────────────────┐
# │ ⏱️ Rate Limiting                                        │
# └─────────────────────────────────────────────────────────┘
RATE_LIMIT_ENABLED=true
RATE_LIMIT_FREE_TIER=10  # Requests per hour
RATE_LIMIT_STANDARD=100
RATE_LIMIT_ENTERPRISE=1000
RATE_LIMIT_POWER_USER=500

# ┌─────────────────────────────────────────────────────────┐
# │ 🔒 Security Settings                                    │
# └─────────────────────────────────────────────────────────┘
SECRET_KEY=generate-a-secure-random-key-here-min-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Settings
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
CORS_ALLOW_CREDENTIALS=true

# Encryption
ENCRYPTION_KEY=generate-fernet-key-here-use-python-cryptography
HASH_ALGORITHM=bcrypt
BCRYPT_ROUNDS=12

# ┌─────────────────────────────────────────────────────────┐
# │ 📊 Monitoring & Logging                                 │
# └─────────────────────────────────────────────────────────┘
# Application Insights
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=your-key;IngestionEndpoint=https://australiaeast-1.in.applicationinsights.azure.com/
APPLICATIONINSIGHTS_INSTRUMENTATION_KEY=your-instrumentation-key

# Log Analytics
LOG_ANALYTICS_WORKSPACE_ID=your-workspace-id
LOG_ANALYTICS_SHARED_KEY=your-shared-key

# Azure Sentinel
ENABLE_SENTINEL_INTEGRATION=true

# ┌─────────────────────────────────────────────────────────┐
# │ 🔄 Background Jobs (Celery + Redis)                     │
# └─────────────────────────────────────────────────────────┘
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# Azure Service Bus (alternative for production)
AZURE_SERVICEBUS_CONNECTION_STRING=Endpoint=sb://your-namespace.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=your-key

# ┌─────────────────────────────────────────────────────────┐
# │ 📧 Email Notifications (SendGrid)                       │
# └─────────────────────────────────────────────────────────┘
SENDGRID_API_KEY=SG.your-sendgrid-api-key
EMAIL_FROM=noreply@eig-platform.com.au
EMAIL_FROM_NAME="EIG Platform"
ENABLE_EMAIL_NOTIFICATIONS=true

# ┌─────────────────────────────────────────────────────────┐
# │ 🌐 WebSocket Settings                                   │
# └─────────────────────────────────────────────────────────┘
WEBSOCKET_ENABLED=true
WEBSOCKET_HEARTBEAT_INTERVAL=30  # seconds
WEBSOCKET_MAX_CONNECTIONS=10000

# ┌─────────────────────────────────────────────────────────┐
# │ ⚙️ Model Router Configuration                           │
# └─────────────────────────────────────────────────────────┘
MODEL_SELECTION_STRATEGY=intelligent  # intelligent | cost_optimized | quality_first | manual
DEFAULT_MODEL=azure-openai  # azure-openai | replicate-sdxl | adobe-firefly | azure-ai
ENABLE_MODEL_FALLBACK=true
MODEL_TIMEOUT_SECONDS=60
MAX_RETRY_ATTEMPTS=3
RETRY_BACKOFF_FACTOR=2  # Exponential backoff: 2, 4, 8 seconds

# ┌─────────────────────────────────────────────────────────┐
# │ 🎨 Image Processing                                     │
# └─────────────────────────────────────────────────────────┘
MAX_IMAGE_SIZE_MB=10
IMAGE_QUALITY=85  # JPEG quality 1-100
ENABLE_WATERMARKING=false
WATERMARK_TEXT="Generated by EIG Platform"
THUMBNAIL_SIZE=256  # pixels

# Supported formats
SUPPORTED_OUTPUT_FORMATS=["png","jpg","webp"]
DEFAULT_OUTPUT_FORMAT=png

# ┌─────────────────────────────────────────────────────────┐
# │ 📋 Compliance & Audit                                   │
# └─────────────────────────────────────────────────────────┘
AUDIT_LOG_RETENTION_DAYS=2555  # 7 years (ISO 27001 requirement)
ENABLE_IMMUTABLE_AUDIT_LOGS=true
DATA_RESIDENCY_REGION=australiaeast  # Enforce Australian data sovereignty

# Compliance toggles
ISO27001_MODE=true
GDPR_MODE=true
PRIVACY_ACT_MODE=true

# ┌─────────────────────────────────────────────────────────┐
# │ 🧪 Development & Testing                                │
# └─────────────────────────────────────────────────────────┘
TESTING_MODE=false
MOCK_EXTERNAL_APIS=false  # Set true for local dev without API keys
ENABLE_API_DOCS=true  # Swagger/OpenAPI docs at /docs

# Feature Flags
FEATURE_BATCH_GENERATION=true
FEATURE_PROMPT_TEMPLATES=true
FEATURE_CUSTOM_STYLES=true
FEATURE_API_ACCESS=true

# ⚠️  SECURITY CHECKLIST BEFORE DEPLOYMENT:
# □ Generated strong SECRET_KEY (min 32 random characters)
# □ Generated ENCRYPTION_KEY using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# □ All API keys obtained from providers and stored in Azure Key Vault
# □ Database uses strong password (min 16 chars, mixed case, numbers, symbols)
# □ Azure AD app registered with correct redirect URIs
# □ CORS_ORIGINS updated to production domains
# □ DEBUG=false in production
# □ LOG_LEVEL=WARNING or ERROR in production
# □ MFA enabled for all users
# □ All secrets loaded from Key Vault (not from .env file in production)
#
# 📋 ISO 27001 Control: A.9.4.5 - Access control to program source code
# 📋 ISO 27001 Control: A.12.4.1 - Event logging
